#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic-tac-toe. Start a new match?"
puts "y/n"

answer=gets.strip
new_game(answer)

user_input=gets.strip
input_to_index(user_input)

move(board, input_to_index, token="X")